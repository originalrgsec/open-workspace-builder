# Release v1.1.0 — Sprint 15: Hardening + Drift Detection

**Date:** 2026-03-29
**Commit:** d142097
**Tests:** 1405 (1356 → 1405, +50 new)

## Code

### Modules Added
- `security/drift.py` — Directive drift detection with SHA-256 baseline comparison, file discovery, atomic baseline writes, glob filtering, JSON output
- `security/trust.py` — First-party ECC trust manifest: hash computation for vendor/ecc/ files, `is_trusted()` check for migrate operations

### Modules Modified
- `cli.py` — Added `owb security drift` command with `--update-baseline`, `--json`, `--files`, `--baseline` options
- `engine/builder.py` — Added `_check_vault_nesting()` static method for vault marker detection before build
- `engine/vault.py` — `_write()` and `_deploy_policies()` now skip existing files with `[skip]` output
- `engine/migrator.py` — Trust manifest integration: `_ecc_relative_path()` helper, trusted files bypass security scanning
- `registry/registry.py` — `min_owb_version` gate via `packaging.version.Version`, `_warn_unknown_fields()` for graceful degradation, `_KNOWN_FIELDS` allowlist

## Project Docs
- PRD: All 5 open questions (Q1-Q5) resolved and moved to Resolved Questions section
- SDR: Sprint 13, 14, and 15 entries added; backlog updated; open questions reduced from 3 to 1

## Tests

| Category | Count |
|----------|-------|
| Init safety (S060+S063) | 13 |
| Drift detection (S082) | 21 |
| ECC trust (S061) | 7 |
| Registry versioning (S083) | 9 |
| **Total new** | **50** |

### Test Files Added
- `tests/test_init_safety.py`
- `tests/security/test_drift.py`
- `tests/test_ecc_trust.py`
- `tests/test_registry_versioning.py`

## Stories Completed

| ID | Description | Size |
|----|-------------|------|
| OWB-S060 | Init detect-and-skip existing scaffold files | S |
| OWB-S063 | Init prevents vault nesting when target is vault directory | S |
| OWB-S082 | Directive drift detection (`owb security drift`) | M |
| OWB-S061 | Scanner trusts first-party ECC content during migrate | M |
| OWB-S083 | Registry `min_owb_version` gate + unknown field warnings | S |
