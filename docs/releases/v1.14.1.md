# v1.14.1 — Gate & CVE (Sprint 30)

Released: 2026-04-15

## Summary

Patch release that closes two loose ends from the v1.14.0 push:

1. **Dependabot alert #17** — pytest `< 9.0.3` is vulnerable under
   CVE-2025-71176 (tmpdir handling, medium). Dev-dep only, never shipped in
   the wheel.
2. **Dependency-gate hook argv parser bug** — the previous regex-based parser
   misread shell operators and numeric file descriptors as package names,
   blocking `uv pip install pkg 2>&1 | tail` and `uv add --dev pkg` with
   bogus "package not found" errors. Sprint 29 hit this twice.

No user-facing API or config surface changes. The gate fix is an internal
tooling quality improvement; the CVE fix is dev-dep hygiene.

## Code Changes

### Fixed

| File | Change |
|------|--------|
| `vendor/ecc/hooks/dependency-gate.py` | Rewrote argv parser to use `shlex.split` + `INSTALL_VERBS` prefix match + `SHELL_TERMINATORS` stop set + token classifier. Skips flags, paths (`/`, `./`, `..`), URLs (`git+`, `https://`, `file://`), and command substitution (`$(`, `` ` ``, `${`). `FLAGS_WITH_VALUE` list consumes the following token as value not package |
| `vendor/ecc/hooks/dependency-gate.py` | New `PackageSpec` dataclass with `name` and optional `pinned_version`. `check_pypi()` respects `==X.Y.Z` exact pins in the quarantine check (subsumes himitsubako Sprint 7 follow-up). Range/compat specs (`>=`, `<=`, `~=`, `!=`) keep `pinned_version=None` and fall back to "check latest" — conservative default |
| `vendor/ecc/hooks/dependency-gate.py` | `uv sync` and `uv lock` intentionally absent from `INSTALL_VERBS` (documented inline). Those commands move the installed set to match the existing lock, which was already gated when packages were added |
| `~/.claude/hooks/dependency-gate.py` | Vendored copy synced to the user-active copy (AC-6). Existing operators who did not re-run `owb init` should copy the new vendored file over their active hook |
| `tests/hooks/test_dependency_gate_parser.py` | 48 new tests covering every Sprint 29 regression input plus AC-1 through AC-5 and EC-1, EC-2 |

### Changed

| File | Change |
|------|--------|
| `pyproject.toml` | `pytest>=9.0.3` in `[dev]` extras (was `>=7.0`). Closes CVE-2025-71176 |

### Security

- **CVE-2025-71176** patched via pytest upgrade. Dev-dep only; no production
  surface was affected. GitHub Dependabot alert #17 auto-dismisses on next
  scan.

## Upgrade Notes

- If you are running OWB's dependency-gate hook at `~/.claude/hooks/dependency-gate.py`,
  copy the new vendored file to pick up the parser fix. Future `owb init`
  runs will install the new hook automatically.
- No runtime changes. Existing `workspace.yaml` files and the installed wheel
  behave identically to v1.14.0.

## Stats

- 1,907 tests passing (+48 parser tests from v1.14.0)
- Coverage: 85.18% (parser fully covered; hook's `check_pypi` / `main`
  network paths intentionally not mocked in this story)
- Sprint 30: two stories (OWB-SEC-004, OWB-S133), ~3-4 points
