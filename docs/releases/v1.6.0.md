# v1.6.0 — SBOM Foundation (OWB-S107a)

**Released:** 2026-04-11
**Sprint:** 20
**Tests:** 1,646 (+83 new SBOM tests, from 1,563)
**Theme:** Minimum viable CycloneDX 1.6 SBOM for AI workspace extensions

## Code

| Module | Change |
|--------|--------|
| `src/open_workspace_builder/sbom/__init__.py` | **New** package marker |
| `src/open_workspace_builder/sbom/normalize.py` | **New** — versioned `norm1` algorithm (trailing whitespace + LF + `updated:` stripping) producing `sha256-norm1:<hex>` hash strings |
| `src/open_workspace_builder/sbom/discover.py` | **New** — walks `.claude/skills`, `.claude/agents`, `.claude/commands`, and `.mcp.json`; emits frozen `Component` records |
| `src/open_workspace_builder/sbom/builder.py` | **New** — CycloneDX 1.6 JSON serialization via `cyclonedx-python-lib`, with `BomOptions` for deterministic output |
| `src/open_workspace_builder/sbom/_example.py` | **New** — deterministic regeneration helper for the committed fixture |
| `src/open_workspace_builder/cli.py` | Added `owb sbom` command group with `generate` subcommand. Added `--emit-sbom PATH` flag to `owb scan`. |
| `pyproject.toml` | New `[sbom]` optional extra pinning `cyclonedx-python-lib>=9.0,<11` (promoted from transitive) |
| `uv.lock` | Resolver refresh for the `[sbom]` extra |
| `.gitignore` | Exception added so `tests/fixtures/sbom-example/.claude/` is tracked (CI drift test needs it) |

## Project Docs

| File | Change |
|------|--------|
| `docs/prd.md` | New UC-18: AI Workspace SBOM Generation |
| `docs/adr.md` | New AD-17 (CycloneDX 1.6 as primary SBOM format), new AD-18 (versioned normalization for SBOM content hashing) |
| `docs/sdr.md` | Sprint 20 entry added with full S107a implementation detail |
| `docs/threat-model.md` | No update; S107a is read-only, no new attack surface beyond the existing scanner walk. Threat review deferred to S107c (verify/diff surface). |
| `CHANGELOG.md` | `[1.6.0]` entry added per Keep a Changelog |

## Content

| File | Change |
|------|--------|
| `examples/sbom/example.cdx.json` | **New** — committed CycloneDX 1.6 SBOM generated from the repo fixture |
| `tests/fixtures/sbom-example/.claude/skills/hello/SKILL.md` | **New** fixture skill |
| `tests/fixtures/sbom-example/.claude/agents/example-agent.md` | **New** fixture agent |
| `tests/fixtures/sbom-example/.mcp.json` | **New** fixture MCP server declaration |

## Infrastructure

No CI workflow changes. The CycloneDX drift check runs inside the test suite
as `tests/sbom/test_example_fixture.py::TestExampleSbomDriftCheck`, which CI
already invokes via pytest.

## Tests

| Category | Count | Notes |
|----------|-------|-------|
| `tests/sbom/test_normalize.py` | 27 | norm1 algorithm rules, hash stability, version tag |
| `tests/sbom/test_discover.py` | 21 | Component discovery, hash stability, empty/malformed workspaces |
| `tests/sbom/test_builder.py` | 15 | CycloneDX 1.6 serialization, property shape, schema validation |
| `tests/sbom/test_cli.py` | 9 | `owb sbom generate` command surface |
| `tests/sbom/test_scan_integration.py` | 4 | `owb scan --emit-sbom` integration |
| `tests/sbom/test_workflow_ac.py` | 5 | Workflow-level AC per integration-verification-policy §1 |
| `tests/sbom/test_example_fixture.py` | 2 | Example SBOM drift check + schema validation |
| **Total new** | **83** | |
| **Full suite** | **1,646 passed, 4 skipped** | Up from 1,563 |

SBOM module coverage: **94%** (213 statements, 13 missing).

## Stories Completed

| Story | Size | Description |
|-------|------|-------------|
| OWB-S107a | M (~5 pt) | SBOM schema + discovery + `owb sbom generate` CLI. First slice of the parent S107 story. |

## Deferred to Future Sprints

- **OWB-S107b** (Sprint 21 candidate): git-history + install-record provenance,
  capability extraction (`allowed-tools`, MCP connections, network declarations),
  license detection with `allowed-licenses.md` cross-reference. Also: migration
  from `owb:evidence-path` property to spec-native `evidence.occurrences[].location`
  (blocked on `cyclonedx-python-lib` upgrade past 9.x).
- **OWB-S107c** (Sprint 22 candidate): `owb sbom diff`, `owb sbom verify`,
  `owb sbom show` operational commands; SPDX 2.3 output; S089 package quarantine
  consulting the SBOM for recently-added skills; `docs/concepts/sbom.md` concept
  page; `docs/howto-sbom.md` howto.
- **OWB-SEC-003** (Sprint 21 candidate, filed during this sprint): bump
  `cryptography` 46.0.6 → 46.0.7 for CVE-2026-39892 (Dependabot alert #16).
  Medium-severity buffer overflow in `Hash.update()` on non-contiguous buffers.
  Transitive via `pip-audit`/`keyring`/`pyrage`. In-context risk low.

## Dependencies

- `cyclonedx-python-lib` promoted from transitive (`pip-audit`) to direct in the
  new `[sbom]` optional extra. License: Apache-2.0 (allowed per
  `allowed-licenses.md`). Pinned `>=9.0,<11` — stays on 9.1.0 in the lockfile;
  the 11.x major bump is deferred to S107b/c review.
- New transitive deps installed into the `[sbom]` extra: `boolean-py 5.0`,
  `defusedxml 0.7.1`, `license-expression 30.4.4`, `packageurl-python 0.17.6`,
  `py-serializable 2.1.0`, `sortedcontainers 2.4.0` (already present).

## Notes

- Session budget check: this sprint's single story delivered in one session
  at ~5 pt plus meta-work (planning, backlog scrub, SEC-003 filing, close-out).
  Total load estimated at 7-8 pt, at the 8-point cap from S117. Closeout quality
  held up; no context pressure symptoms observed.
- Integration verification: `tests/sbom/test_workflow_ac.py` exercises the full
  `owb sbom generate` CLI pipeline end-to-end on a 7-component fixture workspace,
  including the hashing stability contract (cosmetic edits → no drift; body
  edits → exactly one hash flips).
- The code was merged to `main` as commit `5ae481f` before the close-out pass
  ran. The version bump, CHANGELOG, SDR entry, and release manifest land in a
  separate `docs:` commit matching the Sprint 18/19 pattern.
