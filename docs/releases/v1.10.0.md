# v1.10.0 — Release Ops Hardening (Sprint 24)

**Released:** 2026-04-11
**Sprint:** 24
**Tests:** 1,859 (+4 net from v1.9.0; 7 new resolver tests)
**Theme:** Harden the release pipeline against the three failure classes Sprint 23 exposed

## Stories Completed

| ID | Title | Size |
|----|-------|------|
| OWB-S121 | Vault errata for ghost releases + DRN-074 no-backfill decision | S (~2 pt) |
| OWB-S120 | Fix `_REPO_ROOT` in `sbom/_example.py` for installed-wheel layout | XS (~1 pt) |
| OWB-S119 | Slack release webhook → `#owb-releases` | S (~2 pt) |

**Total: ~5 pt** (under 8 pt session budget)

## Code

| Module | Change |
|--------|--------|
| `src/open_workspace_builder/sbom/_example.py` | **Rewritten** — path resolution via `importlib.resources.files()` instead of `Path(__file__).parents[3]` (OWB-S120) |
| `src/open_workspace_builder/sbom/_data/example.cdx.json` | **Moved** from `examples/sbom/example.cdx.json` — now package data accessible from installed wheels |
| `src/open_workspace_builder/sbom/_data/fixture/` | **Moved** from `tests/fixtures/sbom-example/` — fixture workspace bundled as package data |
| `pyproject.toml` | Added `sbom/_data/**` to `[tool.setuptools.package-data]` |
| `.gitignore` | Updated `.claude/` exception for new `_data/fixture/` location |

## Infrastructure

| File | Change |
|------|--------|
| `.github/workflows/release-notify-slack.yml` | **New** — Slack notification workflow triggered on `release: published` (OWB-S119) |

## Project Docs

| File | Change |
|------|--------|
| `docs/contributing/release-process.md` | Added Release Notifications section (provisioning, rotation, multi-channel, troubleshooting). Updated Overview to mention stage 5. |
| `docs/releases/v1.6.0.md` | Ghost-release errata banner added (OWB-S121) |
| `docs/releases/v1.7.0.md` | Ghost-release errata banner added (OWB-S121) |
| `docs/releases/v1.8.0.md` | Ghost-release errata banner added (OWB-S121) |
| `docs/sdr.md` | Sprint 24 entry added |
| `CHANGELOG.md` | `[1.10.0]` section added |

## Tests

| Category | Count |
|----------|-------|
| `tests/sbom/test_example_resolver.py` (new) | 7 |
| `tests/sbom/test_example_fixture.py` (existing, passing) | 3 |
| Total suite | 1,859 passed, 6 failed (pre-existing quarantine date-boundary), 1 skipped |

## Dependencies

| Package | Version | Action | Gate |
|---------|---------|--------|------|
| `slackapi/slack-github-action` | v3.0.1 | GitHub Actions `uses:` (not pip) | MIT, GREEN health score, 29 days past quarantine |

No new Python dependencies. `importlib.resources` is stdlib (Python 3.9+).

## Security

No new attack surface. `SLACK_RELEASE_WEBHOOK` stored as GitHub repo
secret with `contents: read` workflow permissions. Webhook grants
write-only access to a single Slack channel. Rotation procedure
documented in `docs/contributing/release-process.md`.

## Known Issues

- 6 quarantine tests exhibit date-boundary sensitivity (fail when run near
  midnight UTC). Pre-existing on main since before Sprint 24. Not caused by
  any Sprint 24 change (verified via `git stash` isolation test).
